import AdminPlaceholder from "@/components/admin/AdminPlaceholder";

export const metadata = { title: "Contact Information" };

export default function AdminWebsiteContactPage() {
  return (
    <AdminPlaceholder
      title="Contact Information"
      description="Manage the public contact details shown on the website."
    />
  );
}
