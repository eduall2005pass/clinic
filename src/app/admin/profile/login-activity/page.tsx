import AdminPlaceholder from "@/components/admin/AdminPlaceholder";

export const metadata = { title: "Login Activity" };

export default function AdminProfileLoginActivityPage() {
  return (
    <AdminPlaceholder
      title="Login Activity"
      description="Review recent sign-in activity for your account."
    />
  );
}
